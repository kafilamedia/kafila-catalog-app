<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="MenuButton">
        <attr name="drawableId" format="integer" />
        <attr name="buttonLabel" format="string" />
    </declare-styleable>
    <declare-styleable name="ImageLabel">
        <attr name="imageLabelDrawableId" format="integer" />
        <attr name="imageLabelText" format="string" />
    </declare-styleable>
    <declare-styleable name="KafilaFooter">
    </declare-styleable>
    <declare-styleable name="KafilaHeader">
    </declare-styleable>
</resources>
